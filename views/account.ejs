<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Your Profile</title>
        <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }

            body {
                font-family: "Helvetica Neue", Arial, sans-serif;
                background: #fafafa;
                color: #333;
                line-height: 1.6;
            }
            .profile-hero {
                background: linear-gradient(135deg, #667eea, #764ba2);
                min-height: 100vh;
                padding: 40px 20px;
            }
            .container {
                max-width: 1200px;
                margin: 0 auto;
                padding: 20px;
            }
            .card {
                background: #fff;
                border-radius: 10px;
                padding: 30px;
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            }
            h1 {
                font-size: 2rem;
                font-weight: 700;
                color: #764ba2;
                margin-bottom: 20px;
            }
            p {
                font-size: 1.1rem;
                margin-bottom: 15px;
            }
            .info-item {
                margin-bottom: 20px;
            }
            a {
                text-decoration: none;
                font-weight: 600;
                color: #764ba2;
                transition: color 0.3s ease;
            }

            a:hover {
                color: #667eea;
            }
        </style>
    </head>
    <body>
        <div class="profile-hero">
            <%- include("partials/loggedInHeader") %>
            <div class="container">
                <div class="card">
                    <h1>Welcome <%= user.name %>, how are you today?</h1>
                    <div class="info-item">
                        <p><strong>Name:</strong> <%= user.name %></p>
                        <a href="/dashboard/account/update/name">Change name</a>
                    </div>
                    <div class="info-item">
                        <p><strong>Username:</strong> <%= user.username %></p>
                    </div>
                    <div class="info-item">
                        <p><strong>Password:</strong> ********</p>
                        <a href="/dashboard/account/update/password"
                            >Change password</a
                        >
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>
